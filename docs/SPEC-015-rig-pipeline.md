# SPEC 015 — Rig pack 动画管线换代（B 路线）

⚖️ 2026-08-25 grill 定稿。PoC 证据链在 `~/Projects/scratch/pet-rig-spike/`（姿势稳定性 10/10、
AI 部件框、全自动骨架链 auto-rig.html 三闸全过）；质量正典 = `pet-rig-spike/canon/`
（`three-1.png` + config.json：gemini-3.1-flash-image，3 图输入）。术语见仓根 `CONTEXT.md`。

> **⚖️2026-08-28 降级(ADR 0002)**:项目收敛为 MSc 毕设,商业化冻结。本 spec 内
> 订阅制、真 IAP 二期、后端生产部署、E 卡(预置 9 只量产)全部 **frozen**——台账保留,
> 不排期不执行。A–D 已落 main 的实装维持现状。

## 背景与目标

v2 逐帧图集（8×11，~14 次串行生成，40 分钟/只）退役。换为：正典姿势 4 张 + 模板骨架程序驱动。
用户上传照片后 **~1 分钟**孵出会呼吸、眨眼、跟随、奔跑的宠物；单只生成成本 ~¥0.3。
app 现状的「日程定格不动」被骨架常驻微动整体取代。

## 已拍板（全部 ⚖️2026-08-25，勿翻案）

- 宪法④修正案：起极简代理后端，app 直连自动孵化；concierge 人肉环退役。
- 商业（⚖️2026-08-26 修订，取代一次性解锁）：9 只猫狗预置免费；「自家宠物」= **订阅制**
  —— US$9.99/月 或 US$69.99/年（年订为 Best Deal 锚），7 天免费试用；订阅即得 3 次孵化
  额度，在订期间每月补 1 次。对标 Finch（£9.99/月）而略低，留内容深度差距。
  实装 V1 仍为本地占位门，真 IAP/Play Billing 二期。
- V1 物种 = 仅猫狗（两套模板骨架：四足坐姿/四足侧视）。非猫狗照片温柔拒绝 + 物种愿望登记。
- intake：1–3 张照片；文案只说「提供不同角度可以提高生成质量」，不指挥拍什么。
- 素材 4 张/只：正面坐姿睁眼、同姿闭眼、蜷卧睡姿、侧视站姿（侧视 V1 进包，不留召回债）。
  **修订（⚖️2026-08-28 David，翻案）**：跨姿势 AI 生成的画风/identity 一致性不可靠，side 降为
  **可选**（side.png 与 rig side 段成对出现或成对缺失）；side 缺失时跑步动作降级为正面蹦跳位移。
  首个三图宠 = Choco（015F）。
- Rig 动作集：呼吸、眨眼、视线/头部跟随、蹭手、开心跳、吃零食、伸懒腰、入睡过渡、跑步（侧视）。
  **failed 类反应永久移除**（零惩罚红线）。
- 存量 v2 宠物双轨照跑，永不失效；Choco/Lynne v3 化不做。
- 预置 7 只新宠 = 柴犬、金毛、柯基、哈士奇、橘猫、黑猫、布偶；纯文字生成，David 圈脸定版。

## 交付单元（按可独立验收拆，≈5 卡）

**A · 生成管线 CLI**（无 app 依赖）
输入 1–3 张照片（或纯文字品种描述）→ 4 张正典姿势（canon 配置为质量基准）→ 去底（纯色
flood-fill）→ 部件框（gemini-3.5-flash，头/尾/前腿，坐姿+侧视各一组）→ 打包 rig pack。
QA 产物：contact sheet + 部件框 overlay。重试与 best-of-2 采样内建。

**B · 代理后端**
接口：提交孵化（照片+petName）→ 任务态（蛋）→ 取包/推送。职责:Gemini key 服务端持有、
物种门（猫狗分类，拒绝时返回物种登记）、配额（每账号 3 次孵化）、限流与成本护栏。
部署复用海外线基建（DO）。iOS/Android 共用。

**C · app 骨架渲染器**
rig pack 解析 + 层合成渲染（正面/侧视两骨架），动作集全量；像素量化（位移整簇、旋转步进）；
与 v2 图集渲染双轨并存，注册表按包格式路由。摸一摸 16 方向 look 语义映射到头部跟随。

**D · app 孵化流程改造**
intake 1–3 张 + 角度文案；IAP 解锁门（占位价）；蛋态轮询/推送「孵化完成」；
非猫狗拒绝文案 + 愿望登记上报；share-sheet 导出/导入通道保留（rig pack 同样可手动导入）。

**E · 预置 9 只生产**（纯管线运营，无代码）
7 只新宠各出 4–6 候选 → David 圈脸 → 全 9 只出 rig pack 内置进 app（Choco/Lynne 保 v2 双轨，
新 7 只直接 rig pack）。

## 测试 seam（先红后绿）

1. **包导入/注册表边界**（复用 008 已有 seam）：v3 识别、v2 双轨、同 id 覆盖、无效包拒绝。
2. **渲染器状态机**：日程时段/事件 → 动作的映射表单测（不测像素，测「此刻该播什么」）；
   骨架驱动参数的确定性快照（同 t 同输出）。
3. **后端 API 契约**：提交→蛋→取包全链;配额扣减与拒绝路径;物种门三态（狗/猫/其他）。
4. **App 全量回归闸**：现有 50 测试保持绿（v2 双轨零回归的机械证明）。

## ADHD 映射（宪法门）

自家宠物=情感移植主假设载体；1 分钟出宠=即时反馈（延迟满足是 ADHD 弃用点）；
预置免费=零门槛激活；一次性解锁=单次决策无订阅焦虑；角度文案不指挥=降低指令负担；
常驻微动=「眼见即存在」的活体感（对位 002 需求侧证据）。

## Out of Scope

002 常驻层集成（骨架跑步素材已备好，集成归 002 波）；缺席语义表达（板 1）；定价数字；
非猫狗物种模板；Choco/Lynne v3 重做；web 端任何形态；v2 生成管线维护（冻结不删）。
