abstract final class PetMotion {
  static const Duration quick = Duration(milliseconds: 200);
  static const Duration task = Duration(milliseconds: 220);
  static const Duration fade = Duration(milliseconds: 300);
  static const Duration unlockSlide = Duration(milliseconds: 450);
  static const Duration hop = Duration(milliseconds: 550);
  static const Duration breath = Duration(milliseconds: 2800);
  static const Duration theaterDelay = Duration(milliseconds: 900);
  static const Duration taskReaction = Duration(milliseconds: 2000);
  static const Duration theaterReaction = Duration(milliseconds: 3200);
  static const Duration unlockVisible = Duration(milliseconds: 2800);
  static const Duration twinkle = Duration(milliseconds: 1800);
  static const double breathScale = 1.035;
}
